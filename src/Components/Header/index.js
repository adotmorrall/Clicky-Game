import React from 'react';
// import './style.css';

function Header() {
    return (
        <header className='header'>
            <h1>Clicky Game!</h1>
            <h1>Click on one of the Simpsons images to score points, but try not to click on the same image more than once!</h1>
        </header>
    );
}

export default Header;